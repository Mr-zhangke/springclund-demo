eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9001/eureka/

server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Mr-zhangke/springcloud-config
          search-paths: config
  application:
    name: config-server
